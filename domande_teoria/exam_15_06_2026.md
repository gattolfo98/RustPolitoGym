## Si consideri il seguente programma Rust:
```rust
struct Resource {
  name: String,
}
impl Resource {
  fn new(name: &str) -> Resource {
    Resource {
      name: String::from(name),
    }
  }
}
impl Drop for Resource {
  fn drop(&mut self) {
    println!(" => Dropping {}", self.name);
  }
}
fn consume_resource(r: Resource) {
  println!("Consuming {}", r.name);
}
fn borrow_resource(r: &Resource) {
  println!("Borrowing {}", r.name);
}
fn main() {
  let r1 = Resource::new("R1");
  {
    let r2 = Resource::new("R2");
    borrow_resource(&r2);
    let r3 = Resource::new("R3");
    consume_resource(r3);
    println!("End inner block");
  }
  borrow_resource(&r1);
  let r4 = Resource::new("R4");
  let _r5 = r4;
  println!("End main");
}
```

1. In quale ordine vengono stampati i messaggi? (1 pt)
2. Quando viene invocato automaticamente il metodo drop? (0.5 pt)
3. Che cosa si intende per RAII? (0.5 pt)
4. Perché questo meccanismo è importante nella programmazione di sistema? (0.5 pt)
5. È possibile chiamare esplicitamente r1.drop()? Motivare la risposta. (0.5 pt)


## Si consideri il seguente codice:
```rust 
use std::sync::{Arc, Mutex};
use std::thread;
fn main() {
  let data = Arc::new(Mutex::new(0));
  let data2 = Arc::clone(&data);
  let handle = thread::spawn(move || {
    let mut value = data2.lock().unwrap();
    *value = 10;
    panic!("errore nel thread");
  });
  let _ = handle.join();
  // Domanda 3: si fa riferimento a questo unwrap()
  // Domanda 4: si sostituisca questa linea di codice
  let value = data.lock().unwrap();
  println!("{}", *value);
}
```

1. Il codice compila? (0.25 pt)
2. Che cosa succede a runtime? (1.25 pt)
3. Perché la chiamata unwrap() sul secondo lock() (dopo i commenti) può causare un panic? (0.5 pt)
4. Come si può evitare questo panic? Si scriva un frammento di codice opportuno (si veda il commento).

## Domanda multipla
### Prima parte 
Analizzare il seguente programma:
```rust 
fn main() {
  let x = 10;
  let c1 = || println!("{}", x);
  let mut y = 0;
  let mut c2 = || {
    y += 1;
    println!("{}", y);
  };
  let s = String::from("hello");
  let c3 = || {
    drop(s);
  };
}
```

Considerando i trait Fn, FnMut e FnOnce.
1. Quale trait implementa c1? Si motivi la risposta. (0.5 pt)
2. Quale trait implementa c2? Si motivi la risposta. (0.5 pt)
3. Quale trait implementa c3? Si motivi la risposta. (0.5 pt)


### Seconda parte
Si conderi il seguente programma che non viene correttamente compilato:
```rust 
use std::thread;
fn main() {
  let message = String::from("Hello");
  let handle = thread::spawn(|| {
    println!("T2: {}", message);
  });
  println!("T1: {}", message);
  handle.join().unwrap();
}
```

1. Si spieghino le problematiche relative al suddetto codice. (0.75 pt)
2. Correggere il codice per permettere di eseguire correttamente le 2 stampe (alle righe 5 e 7). (0.75 pt)

